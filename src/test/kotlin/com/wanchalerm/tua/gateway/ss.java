package com.wanchalerm.tua.gateway;

public class ss {

    void sww() {
        messageBodyDecoders.stream().collect(Collectors.toMap(MessageBodyDecoder::encodingType, Function.identity()));
    }

}
